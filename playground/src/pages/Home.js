import { Grid,TextField } from '@mui/material'
import React from 'react'

const Home = (props) => {
    return (
        <Grid>
            <TextField label="Name" />
        </Grid>
    )
}

export default Home
