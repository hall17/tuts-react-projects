const DEFAULT_LANGUAGE = 'en';

const ROUTES = {
    INDEX: '/',
    DEFAULT: '/',
    LOGIN: '/login',
    LANDING_PAGE: '/landing-page',
    PROFILE: '/profile'
};

export {
    DEFAULT_LANGUAGE,
    ROUTES
}